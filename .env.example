# Temporary local SDK checkout. Build it with `npm run build:sdk` there first.
ATLAS_MODERNIZATION_PATH="/absolute/path/to/Atlas Modernization"
ATLAS_BASE_URL=https://atlas.example.test
ATLAS_API_KEY=replace-with-an-atlas-api-key
